<%= provide(:title, @category.name) %>

<h2 class="tags-title"><%= @category.name %></h2>

<%= render 'shared/card_list_layout', title: @category.name, iteration: @posts  %>

<%= will_paginate @posts, inner_window: 3, outer_window: 3 %>
